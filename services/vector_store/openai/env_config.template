# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vector_store_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Channel Manager Configuration
CHANNEL_MANAGER_API_KEY=your_channel_manager_api_key_here


#log
LOG_URL=vector_store_service.log
IS_PRODUCTION=no

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Update the values with your actual configuration
# 3. Make sure PostgreSQL is running and the database exists
# 4. Install required dependencies: pip install psycopg2-binary python-dotenv 